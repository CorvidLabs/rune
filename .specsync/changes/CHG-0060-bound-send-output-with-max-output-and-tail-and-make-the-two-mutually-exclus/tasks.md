---
change: CHG-0060-bound-send-output-with-max-output-and-tail-and-make-the-two-mutually-exclus
artifact: tasks
---

# Tasks

- [x] `exchange` accepts `options:` and routes through `bounded_output`
- [x] `bounded_output` bounds raw, derives clean, merges the omitted counts
- [x] `conflicting_bounds` in `extract_options`, message shared with `rune run`
- [x] Four tests, each verified against deliberately unfixed code
- [x] `session.spec.md`: two export rows, invariants 50 / 50a / 50b
- [x] `ROADMAP.md`: re-measure the 0.8.0 table, correct the oversized-send entry
