---
change: CHG-0015-record-exact-supersession-for-the-committed-cli-help-contract-and-document-cli-r
artifact: context
---

# Context

Several accepted changes touched the exact CLI canonical-spec delivery input while the help branch
was being restacked. CHG-0015 is based on commit `e433ab7`, which contains the complete accepted
contract stack. It adds the missing reused-CLI error-case description as the final semantic change.

Because the earlier changes were accepted before explicit succession was declared, their recovery
uses audited reopen, fresh verification, and closing approval against this final contract.
