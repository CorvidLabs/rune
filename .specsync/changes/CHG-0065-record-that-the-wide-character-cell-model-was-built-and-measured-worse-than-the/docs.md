---
change: CHG-0065-record-that-the-wide-character-cell-model-was-built-and-measured-worse-than-the
artifact: docs
---

# Docs

`parsers.spec.md` invariant 17 and `harnesses/renderer_gaps.rb`. ROADMAP still
lists five renderer gaps and now overstates the problem by four; that entry is
left for the next release commit rather than edited here, where touching the
file would stale accepted changes.
