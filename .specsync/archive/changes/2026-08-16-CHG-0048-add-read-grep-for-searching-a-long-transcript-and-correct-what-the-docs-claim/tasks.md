---
change: CHG-0048-add-read-grep-for-searching-a-long-transcript-and-correct-what-the-docs-claim
artifact: tasks
---

# Tasks

- [x] `--grep` and `--context` on `read`, matching cleaned text
- [x] `grep_matches`, and `grep_error` instead of raising
- [x] `prompt_detected` documented with the measured table
- [x] `settled`'s third case documented
- [x] `dropped_bytes` clarified as a count that does not invalidate a cursor
- [x] regression tests
